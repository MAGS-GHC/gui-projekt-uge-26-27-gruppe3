/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

module.exports = { compiler: { styledComponents: true } };

module.exports = {
    experimental: {
        serverActions: true,
    },
};
